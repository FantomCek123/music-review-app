services:
  backend:
    build:
      context: ./backend
    container_name: backend
    ports:
      - "5000:5000"
    environment:
      MONGO_URI: "mongodb+srv://stricojebci:YyNovk2Ygv7cuvFo@cluster0.hamavtb.mongodb.net/test_db?retryWrites=true&w=majority"
      PORT: "5000"
      JWT_SECRET: "moja_tajna"
      NEXTAUTH_URL: "http://localhost:5000/"
      EMAIL_USER: "filipficanetherlands@gmail.com"
      EMAIL_APP_PASS: "cqxe ttwj nada sjto"
    restart: always

  frontend:
    build:
      context: ./frontend
    container_name: frontend
    ports:
      -  "3000:80"
    environment:
     PORT: "3000"
    depends_on:
      - backend
    restart: always